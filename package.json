{
    "name": "strichliste-backend",
    "private": true,
    "scripts": {
        "format": "prettier --write assets templates",
        "format:check": "prettier --check assets templates"
    },
    "devDependencies": {
        "@zackad/prettier-plugin-twig": "^0.17.0",
        "prettier": "^3.8.4",
        "prettier-plugin-jinja-template": "^2.2.0"
    }
}
